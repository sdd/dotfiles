# Managed by chezmoi — do not edit. See github.com/sdd/dotfiles/README.md#profile
# Shared login-shell config lives in ~/.profile.
if [ -f "$HOME/.profile" ]; then
  # shellcheck source=/dev/null
  . "$HOME/.profile"
fi
